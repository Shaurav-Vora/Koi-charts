,,,koi charts -- command reference,'

,with local commands enabled1 the
  supported phrases below are processed
  without a ,gemini request4 ,other
  phrasing is sent to ,gemini for
  interpretation and may require
  clarification4

,,three ,,rules

#a4 ,join up to ten exact local edits
  with 8then0 or 8and,74 ,if any part is
  uncertain1 the whole request goes to
  ,gemini4
#b4 ,quote a label that contains grammar
  words3 add a process called 8,check
  before payment,74
#c4 ,name a shape by its own label ",-
  or1 when the chart has only one of
  them1 by its kind3 8the decision,74
#d4 ,use its short reference when a
  label is tiring to say3 node #c or
  ,n#c4 ,the reference remains attached
  when the shape is renamed4

,,combine ,,edits

  `<command`> then `<command`>
  ,runs a sequence as one local1
    undoable change when every part
    matches the local grammar4
  ,also3 then2 and then2 and before a
    clear command verb2 up to ten edits4
  ,say3 add a start then add a process

,,,build the chart,'

  add a `<shape`>
  ,adds a shape with a default label4
  ,also3 add2 create2 insert2 make2
    place2 put2 new2 shape3 start _/
    process _/ decision _/ end2 start
    also3 begin2 process also3 step1
    action1 task2 decision also3 choice1
    question2 end also3 finish1 stop1
    terminal4
  ,say3 add a start
  ,say3 create a decision node
  add a `<shape`> called `<label`>
  ,adds a shape with the label you say4
  ,also3 called2 named2 labelled2
    titled2 that says4
  ,say3 add a process called ,check
    payment
  ,say3 add a decision called ,approved8
  ,say3 add a process called 8,check
    before payment0

  add a `<shape`> .<called `<label`>.>
    `<relation`> `<existing shape`>
  ,adds a shape in a chosen position
    beside one already in the chart4
  ,also3 before2 after2 above "<over">2
    below "<under">2 left of2 right of4
  ,say3 add an end below ,check payment
  ,say3 add a process called ,refund to
    the right of ,check payment

,,connect ,,shapes

  connect `<source`> to a new `<shape`>
    .<labelled `<label`>.>
  ,creates the destination1 then
    connects the source to it4 ,labelled
    names the new node1 not the arrow4
    ,a new node without a shape type
    defaults to ,process4 ,undo removes
    both changes4
  ,also3 called2 named2 labeled2
    labelled4
  ,say3 connect ,start to a new process
  ,say3 connect it to a new decision
    labelled ,approved8
  ,say3 connect ,start to a new node
    labelled ,review

  connect `<shape`> to `<shape`>
  ,draws an arrow from the first shape
    to the second4
  ,also3 connect2 link2 join2 draw an
    arrow from4
  ,say3 connect ,start to ,check payment
  ,say3 draw an arrow from ,check
    payment to ,end

  connect `<shape`> to `<shape`>
    labelled `<label`>
  ,draws an arrow and labels it ",- how
    a decision's branches are named4
  ,also3 labelled2 called2 named2 with
    label4
  ,say3 connect ,approved to ,refund
    labelled ,no

  label connection from `<source`> to
    `<destination`> as `<label`>
  ,adds or replaces the label on an
    existing arrow4 ,use full shape
    labels or short references such as
    node #a and ,n#b4
  ,also3 connection2 arrow2 link2 edge2
    as2 with label2 labelled4
  ,say3 label connection from ,approved
    to ,refund as ,no
  ,say3 label arrow from node one to
    ,n#b as ,yes

  clear label on connection from
    `<source`> to `<destination`>
  ,removes an arrow label without
    deleting the connection4
  ,say3 clear label on connection from
    ,n#a to ,n#b

,,,change what is there,'

  rename `<shape`> to `<label`>
  ,changes a shape's label4
  ,also3 rename2 relabel2 change2 to or
    into2 this _/ it _/ the selected
    shape refer to the current
    selection4
  ,say3 rename ,check payment to ,take
    payment
  ,say3 change ,process #c into ,review
  ,say3 rename this to ,take payment

  move `<shape`> `<relation`> `<shape`>
  ,repositions a shape beside another
    one4
  ,say3 move ,refund below ,approved

  delete `<shape`>
  ,removes a shape4 ,a shape with arrows
    asks for confirmation first4
  ,also3 delete2 remove2 erase4
  ,say3 delete ,refund
  ,say3 remove the selected shape

  `<any command`> the `<shape kind`>
  ,when the chart has only one shape of
    a kind1 its kind names it ",- useful
    when the label is a long question4
    ,with two of them you are asked
    which4
  ,also3 the start2 the process "<step1
    action1 task">2 the decision
    "<choice1 question">2 the end
    "<finish1 stop">4
  ,say3 delete the decision
  ,say3 focus on the end

  delete the connection from `<shape`>
    to `<shape`>
  ,removes one arrow1 leaving both
    shapes in place4
  ,also3 connection2 arrow2 link2 edge4
  ,say3 delete the connection from
    ,start to ,check payment

,,,test the chart,'

  test chart _/ start test
  ,starts guided playback at a ,start
    shape without changing the chart4
  ,say3 test chart
  ,say3 start test

  repeat _/ restart test _/ stop test
  ,repeats the current step1 starts the
    route again1 or closes guided
    playback4
  ,also3 repeat step4
  ,say3 repeat
  ,say3 restart test
  ,say3 stop test

  next _/ back _/ take `<branch`>
  ,while testing1 moves through the
    recorded route or takes only the
    branch you name4
  ,say3 next
  ,say3 back
  ,say3 take ,yes

,,,move the cursor,'

  next _/ back
  ,walks one step along the arrows1 and
    says where you landed4
  ,also3 next "<go next1 forward">2 back
    "<go back1 previous">4
  ,say3 next
  ,say3 go back

  take `<branch`>
  ,at a decision1 follows the arrow with
    that label4
  ,also3 take2 follow4
  ,say3 take ,no

  go to start _/ go to end _/ where am
    ,i
  ,jumps to either end of the chart1 or
    repeats your current position4
  ,say3 go to start
  ,say3 go to the end
  ,say3 where am ,i

  focus on `<shape`> _/ clear selection
  ,selects a shape by name1 or drops the
    selection entirely4
  ,also3 focus on2 focus2 select2
    highlight2 clear selection2
    deselect4
  ,say3 focus on ,check payment
  ,say3 select the last shape
  ,say3 clear selection

,,,ask about the chart,'

  describe the chart _/ describe this
  ,reads out the whole chart1 or just
    the selected shape4
  ,say3 describe the chart
  ,say3 describe this

  inspect `<shape`>
  ,lists what leads into and out of one
    shape4
  ,say3 inspect ,check payment
  ,say3 inspect this

  trace the path from `<shape`> .<to
    `<shape`>.>
  ,follows the route between two shapes1
    or onward from one4
  ,say3 trace the path from ,start to
    ,end
  ,say3 trace the path from ,start

  check chart _/ validate the chart
  ,opens a structured local review of
    unfinished or ambiguous chart
    structure4
  ,also3 check the chart4
  ,say3 check chart
  ,say3 validate the chart

  next issue _/ previous issue _/ repeat
    issue _/ close check
  ,moves through1 repeats1 or closes the
    current chart review4
  ,say3 next issue
  ,say3 previous issue
  ,say3 repeat issue
  ,say3 close check

,,,arrange the canvas,'

  arrange chart
  ,repositions the whole chart1 fits it
    into view1 and centres the selected
    shape4
  ,also3 arrange the chart2 auto arrange
    chart2 auto arrange the chart4
  ,say3 arrange chart
  ,say3 auto arrange the chart

  use compact nodes _/ use standard
    nodes
  ,switches between smaller compact
    shapes and the standard readable
    shape size4
  ,also3 compact nodes2 turn compact
    mode on2 turn compact mode off4
  ,say3 use compact nodes
  ,say3 use standard nodes

,,,keep an editable project,'

  save project
  ,downloads the current chart as an
    editable ,koi project file without
    using ,gemini4
  ,also3 export project2 download
    project4
  ,say3 save project
  ,say3 export project
  ,say3 download project

  open project
  ,moves focus to ,open project4 ,press
    ,enter to choose a ,koi file2
    browsers require that final keyboard
    action4
  ,also3 import project2 load project4
  ,say3 open project
  ,say3 import project
  ,say3 load project

,,,take it back,'

  undo _/ redo
  ,steps back or forward through your
    edits4 ,walking the chart is not an
    edit4
  ,say3 undo
  ,say3 redo

  confirm delete _/ cancel
  ,confirms or cancels a pending
    deletion4 ,confirmation is
    recognised word for word only1 never
    approximately4
  ,also3 confirm2 confirm deletion2 yes1
    delete it4
  ,say3 confirm
  ,say3 confirm delete
  ,say3 confirm deletion
  ,say3 yes1 delete it
  ,say3 cancel

,,,sent to be interpreted,'

  ,say3 add three steps for onboarding
  ,requires interpretation of the
    requested number and labels of new
    nodes4

  ,say3 delete everything
  ,requests a bulk operation4
    ,destructive changes require
    confirmation4

  ,say3 do not add a start node
  ,contains negation and is not treated
    as a local add command4

  ,say3 move ,start somewhere sensible
  ,does not specify a destination or a
    position relative to another node4
